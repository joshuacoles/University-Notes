---
marginnote: marginnote3app://note/0B06D857-7B2B-4E86-A32F-9393B870EF77
---

Given an integral in one coordinate system $(x, y)$, it can be advantageous to express it instead in a different coordinate system $(u, v)$, with relation,

$$
\begin{align*}
x &= x(u, v) \\
y &= y(u, v).
\end{align*}
$$

To do this we can compute the [[Jacobian]] of the map $R: (u, v) \to (x, y)$, employing it as so,

$$
\iint_\Omega f(x, y) \,\d x\d y =
\iint_\Omega f(x(u, v), y(u, v))
\left|
\frac{\partial(x, y)}{\partial(u, v)}
\right| \,\d u \d v
$$

where the [[Jacobian]] is defined by,


$$
\left|
\parfrac{(x, y)}{(u, v)}
\right| = \begin{vmatrix}
\parfrac{x}{u} & \parfrac{x}{v} \\
\parfrac{y}{u} & \parfrac{y}{v}
\end{vmatrix}.
$$

> Note that the order of $(u, v)$ matters  as it will change the handedness of the coordinate space and also the order of integration (I believe?).


## Specialisation to $\R\to\R$ result

Given a single variable coordinate change, the [[Jacobian]] simplifies to the following,

$$
\left|
\parfrac{(x)}{(u)}
\right| =
\begin{vmatrix}
\parfrac{x}{u}
\end{vmatrix} = \frac{\d x}{\d u}
$$

which one can clearly see provides the traditional change in variable formula.
