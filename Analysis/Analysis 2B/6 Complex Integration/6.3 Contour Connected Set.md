# 6.3 Contour Connected Set

A set $S \sub \C$ is called **Contour Connected** if for any two points $w, z \in S$, there exists a [[6.1 Contours|Contour]] in $S$ with initial point $w$ and terminal point $z$.